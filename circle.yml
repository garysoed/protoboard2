dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install google-chrome-stable
    - cd node_modules/gulp-typescript/node_modules; ln -s ../../typescript typescript

test:
  override:
    - node_modules/.bin/gulp test
